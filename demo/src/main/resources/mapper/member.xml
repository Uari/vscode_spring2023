<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.mapper.MemberMapper">

  <select id="currentTime" parameterType="string" resultType="string">
    SELECT to_char(sysdate, 'YYYY-MM-DD') ctime FROM dual
  </select>

  <select id="memberList" parameterType="map" resultType="map">
    SELECT mem_no, mem_id, mem_pw, mem_email, mem_name, zipcode, address, gubun, mem_picture FROM member2023
    <where>
			<if test="mem_no!=null">AND mem_no=#{mem_no}</if>
			<if test="gubun!=null">
				<choose>
					<when test='gubun.equals("mem_id")'>
						AND mem_id LIKE '%'||#{keyword}||'%'
					</when>
					<when test='gubun.equals("mem_name")'>
						AND mem_name LIKE '%'||#{keyword}||'%'
					</when>
					<when test='gubun.equals("address")'>
						AND address LIKE '%'||#{keyword}||'%'
					</when>
				</choose>
			</if>
		</where>
		order by mem_no desc
  </select>

  <insert id="memberInsert" useGeneratedKeys="true" parameterType="map" keyColumn="mem_no" keyProperty="mem_no">
    insert into member2023(mem_no, mem_id, mem_pw, mem_email, mem_name, zipcode, address, gubun, mem_picture)
    values(seq_member2023_no.nextval, #{mem_id, jdbcType=VARCHAR}, #{mem_pw, jdbcType=VARCHAR}, 
                                      #{mem_email, jdbcType=VARCHAR}, #{mem_name, jdbcType=VARCHAR}, 
                                      #{zipcode}, #{address}, 
                                      #{gubun, jdbcType=VARCHAR}, #{mem_picture, jdbcType=VARCHAR})
  </insert>

  <!-- <update id="memberUpdate" parameterType="map" >
      UPDATE member2023
          SET mem_pw = #{mem_pw, jdbcType=VARCHAR},
              mem_name = #{mem_name, jdbcType=VARCHAR},
              zipcode = #{zipcode, jdbcType=VARCHAR},
              address = #{address, jdbcType=VARCHAR},
              mem_email = #{mem_email, jdbcType=VARCHAR}
        WHERE mem_no = #{mem_no} 
  </update> -->
    <update id="memberUpdate" parameterType="map" >
      UPDATE member2023
          SET gubun = 0,
      <if test='mem_pw != null and mem_pw.length > 0'>
        mem_pw = #{mem_pw, jdbcType=VARCHAR},
      </if>
      <if test='mem_name != null and mem_name.length > 0'>
        mem_name = #{mem_name, jdbcType=VARCHAR},
      </if>
      <if test='zipcode != null and zipcode.length > 0'>
        zipcode = #{zipcode},
      </if>
      <if test='address != null and address.length > 0'>
        address = #{address},
      </if>
      <if test='mem_email != null and mem_email.length > 0'>
        mem_email = #{mem_email, jdbcType=VARCHAR},
      </if>
      <if test='mem_no != null and mem_no.length > 0'>
        WHERE mem_no = #{mem_no} 
      </if>
    </update>

<!-- 
  회원등록시 사진을 세장 추가할 수 있다.
  public int fileInsert<List<Map<String,Object>> picList)
-->
  <update id="fileInsert" parameterType="list">
    INSERT ALL
    <foreach collection="list" item="item" index="index" separator=" ">
      INTO common_file(file_no, file_name, file_size) VALUES (#{item.file_no}, #{item.file_name}, #{item.file_size})
    </foreach>
    SELECT * FROM DUAL
  </update>

</mapper>